{
  "name": "x402",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "typescript/packages/*"
  ],
  "scripts": {
    "prepare": "npm run -ws --if-present build"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "tronweb": "^6.2.0",
    "viem": "^2.45.2"
  }
}
